# Force LF line endings, needed for Docker to work on Windows
# Explicitly enforce LF for shell scripts — CRLF breaks bash execution
*.sh text eol=lf

# Force LF line endings for all text files (cross-platform: Windows + Linux)
* text=auto eol=lf
